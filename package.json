{
  "name": "construction-ai-terminal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test:openapi": "ruby scripts/validate_openapi.rb"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
